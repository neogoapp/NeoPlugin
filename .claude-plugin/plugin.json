{
  "name": "neoplugin",
  "version": "1.9.0",
  "description": "Connect Claude to Neo — your AI agent running outside the Matrix. A thin gateway: multi-agent workflows and utilities are served on demand by the NeoGo MCP server (BYO Anthropic account, OAuth).",
  "author": {
    "name": "NeoGoApp"
  },
  "userConfig": {
    "neogo_mcp_url": {
      "type": "string",
      "title": "NeoGo MCP endpoint",
      "description": "Leave the default (production). Only change it for NeoGo homologation.",
      "default": "https://mcp.neogo.app"
    }
  }
}
